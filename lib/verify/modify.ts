import { VerificationErrors } from "../definitions";

export default function modifyForms(errors: VerificationErrors[]) {
  // calculate team averages of: autocoral, teleopcoral, and processor

  // get teams at events
  // map teams and pull their forms
  // calculate averages and add them to averages object

  // map through verification errors
  // 
}